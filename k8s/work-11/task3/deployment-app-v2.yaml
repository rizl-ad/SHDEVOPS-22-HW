apiVersion: apps/v1
kind: Deployment
metadata:
  name: work11-t3-deployment-app-v2
  labels:
    app: work11-t3-app
spec:
  # replicas: 3
  selector:
    matchLabels:
      app: work11-t3-app-v2
  template:
    metadata:
      labels:
        app: work11-t3-app-v2
    spec:
      containers:
        - image: nginx
          name: nginx
          volumeMounts:
            - name: work11-t3-vol
              mountPath: /usr/share/nginx/html
      volumes:
        - name: work11-t3-vol
          configMap:
            name: work11-t3-configmap
            items:
              - key: index-v2.html
                path: index.html
